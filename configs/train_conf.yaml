dataset:
  path: data/raw/qm9_dataset.csv
  target_columns: [HOMO, LUMO]
  val_fraction: 0.1
surrogate:
  device: directml
  n_models: 5
  epochs: 120
  batch_size: 64
  lr: 0.001
  weight_decay: 0.0001
  dropout: 0.1
  loss: mae
  save_dir: models/surrogate
